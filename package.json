{
  "name": "absenceflow",
  "version": "1.0.0",
  "private": true,
  "description": "AbsenceFlow - Monorepo startup orchestrator",
  "scripts": {
    "dev": "concurrently -k -p \"{name}\" -c \"cyan,green,yellow\" -n \"FRONTEND,BACKEND,AI-SERVICE\" \"npm run dev:frontend\" \"npm run dev:backend\" \"npm run dev:ai\"",
    "dev:frontend": "cd frontend && npm run dev",
    "dev:backend": "cd backend && npm run dev",
    "dev:ai": "cd backend && ./venv/bin/python local_ai_service.py",
    "install:all": "npm install && cd frontend && npm install && cd ../backend && npm install",
    "postinstall": "cd frontend && npm install && cd ../backend && npm install"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
