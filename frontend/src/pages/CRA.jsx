import CRAWorkspace from '../features/cra/CRAWorkspace';

/**
 * CRA (Compte Rendu d'Activité) Page wrapper.
 * Renders the new modular WinSAP CRA workspace for employees and managers.
 */
const CRA = () => {
  return <CRAWorkspace />;
};

export default CRA;
